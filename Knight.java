import  java.lang.Math.*;

public class Knight extends Piece{
	public Knight(Point k){
		super("Knight", k);
	}
	public void move(Point k){
		if((super.k.getY()-j)/2 == super.k.getX()-i && k.getY() !> 8 k.getX() !> 8){
			//moves piece to loc
		}
		if(super.k.getY()-j == (super.k.getX()-i)/2 && k.getY() !> 8 k.getX() !> 8){
			//move piece to loc
		}
	}
}